# BSP SDK Python — Runtime dependencies (mirrors pyproject.toml)
pynacl>=1.5.0
requests>=2.31.0
httpx>=0.27.0
aptos-sdk>=0.9.0
pydantic>=2.0
cryptography>=41.0

# Dev dependencies (install with: pip install -r requirements.txt)
pytest>=8.0
pytest-cov>=4.0
mypy>=1.8
ruff>=0.3
